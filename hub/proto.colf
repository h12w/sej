package hub

type (
	RequestTitle struct {
		Verb       uint8
		ClientID   text
	}

	Put struct {
		JournalDir text
	}
	
	Get struct {
		JournalDir text
		Offset     uint64
	}

	Quit struct {
		JournalDir text
	}

	Response struct {
		Err        text
	}
)