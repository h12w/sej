package wire

type (
	Request struct {
		ID         uint64
		Type       uint8
		ClientID   text
		JournalDir text
		Offset     uint64
	}
	Response struct {
		RequestID  uint64
		Err        text
	}
)

